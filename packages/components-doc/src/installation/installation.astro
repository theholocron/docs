---
import type { InstallationProps } from "./types.ts";

type Props = InstallationProps;

const { package: pkg, dev = false } = Astro.props;
const command = `pnpm add${dev ? " -D" : ""} ${pkg}`;
---

<h2>Install</h2>
<pre><code>{command}</code></pre>
